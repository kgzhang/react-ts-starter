import * as React from 'react'
import { hot } from 'react-hot-loader'

function Login() {
    return (
        <div>
            <h1>Login</h1>
        </div>
    )
}

export default hot(module)(Login)
