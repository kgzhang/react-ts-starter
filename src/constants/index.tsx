export enum COOKIE_KEYS {
    TOKEN = 'token'
}

export enum LOCALSTORAGE_KEYS {
    USERINFO = 'userinfo',
    NAV_OPEN_KEYS = 'navOpenKeys',
    SIDE_BAR_THEME = 'sideBarTheme',
    SIDE_BAR_COLLASPED = 'sideBarCollapsed'
}

export const LOGIN_CATEGORY = ['user', 'admin']
